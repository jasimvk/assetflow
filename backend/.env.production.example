# Production Environment Variables for Backend
# Copy this file to .env.production and fill in your values

# Supabase Configuration (Required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Configuration (Required - add your frontend URL)
CORS_ORIGIN=https://your-frontend-url.vercel.app

# Azure AD Configuration (Optional - for production auth)
AZURE_CLIENT_ID=your_azure_client_id
AZURE_CLIENT_SECRET=your_azure_client_secret
AZURE_TENANT_ID=your_azure_tenant_id

# Security
JWT_SECRET=your_random_secret_key_here

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Instructions:
# 1. Get Supabase service role key from: Settings → API → service_role (⚠️ Keep this secret!)
# 2. Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. Update CORS_ORIGIN after deploying frontend
